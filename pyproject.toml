[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mlh-copilot-todo"
version = "0.1.0"
description = "Minimal CLI todo manager"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[project.scripts]
todo = "run:main"